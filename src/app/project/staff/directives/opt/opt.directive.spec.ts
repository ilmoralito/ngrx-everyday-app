import { OptDirective } from './opt.directive';

describe('OptDirective', () => {
  it('should create an instance', () => {
    const directive = new OptDirective();
    expect(directive).toBeTruthy();
  });
});
