import { FourDirective } from './four.directive';

describe('FourDirective', () => {
  it('should create an instance', () => {
    const directive = new FourDirective();
    expect(directive).toBeTruthy();
  });
});
